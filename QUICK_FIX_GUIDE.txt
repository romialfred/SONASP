====================================================================
QUICK FIX - Sales Constraint Error
====================================================================

ERROR:
------
"new row violates check constraint sales_status_check"
Even though status = "pending_approval" (which is VALID!)

ROOT CAUSE:
-----------
Multiple conflicting constraints OR old cached constraint definition

SOLUTION:
---------
Run the UPDATED script: FIX_SALES_CONSTRAINT_IMMEDIATE.sql

What it does:
1. Drops ALL constraints with "status" in name
2. Cleans invalid status values
3. Verifies all data is valid
4. Creates NEW constraint: sales_status_check_v2
5. Validates constraint logic (NO insert test to avoid column errors)
6. Shows final status distribution

STEPS:
------
1. Open Supabase SQL Editor

2. Copy ENTIRE content of:
   FIX_SALES_CONSTRAINT_IMMEDIATE.sql

3. Paste and Execute

4. Watch for these messages:
   ✓ "Dropped constraint: sales_status_check"
   ✓ "All status values are valid (X rows checked)"
   ✓ "Constraint validation: pending_approval is VALID ✓"
   ✓ "All constraint validations passed successfully"

5. Verify with:
   SELECT conname, pg_get_constraintdef(oid)
   FROM pg_constraint
   WHERE conname = 'sales_status_check_v2';

EXPECTED RESULT:
----------------
✓ Constraint sales_status_check_v2 exists
✓ Validates 9 status values
✓ New sales inserts work
✓ No more constraint errors

CHANGES FROM PREVIOUS VERSION:
-------------------------------
- Removed INSERT test (was failing on missing columns)
- Added logic validation test instead
- Tests multiple status values without inserting data
- Safer and doesn't require knowing exact column names

TIME TO FIX:
------------
< 2 minutes

====================================================================
