╔══════════════════════════════════════════════════════════════╗
║  BATCH DOCUMENTS MIGRATION - READY TO APPLY ✅               ║
╔══════════════════════════════════════════════════════════════╗

FICHIER À UTILISER:
==================
📄 supabase/migrations/20251105000001_create_batch_documents_fixed.sql

CE FICHIER EST COMPLET ET TESTÉ! ✅


ÉTAPES D'APPLICATION:
=====================

1️⃣  OUVRIR SUPABASE DASHBOARD
   → Aller sur: https://supabase.com/dashboard
   → Sélectionner votre projet

2️⃣  OUVRIR SQL EDITOR
   → Menu gauche: "SQL Editor"
   → Cliquer sur "New query"

3️⃣  COPIER LA MIGRATION
   → Ouvrir le fichier:
     supabase/migrations/20251105000001_create_batch_documents_fixed.sql
   
   → Sélectionner TOUT le contenu (Ctrl+A / Cmd+A)
   → Copier (Ctrl+C / Cmd+C)

4️⃣  COLLER ET EXÉCUTER
   → Dans le SQL Editor, coller (Ctrl+V / Cmd+V)
   → Cliquer sur le bouton "Run" (en bas à droite)
   → Attendre ~2-3 secondes

5️⃣  VÉRIFIER LE SUCCÈS
   Vous devriez voir:
   ✅ "Success. No rows returned"
   
   OU des messages comme:
   ✅ "CREATE TABLE"
   ✅ "CREATE INDEX"
   ✅ "CREATE POLICY"


VÉRIFICATION RAPIDE:
====================

Exécutez cette query pour vérifier:

SELECT 
  table_name,
  COUNT(*) as column_count
FROM information_schema.columns
WHERE table_name = 'batch_documents'
GROUP BY table_name;

Résultat attendu:
✓ table_name: batch_documents
✓ column_count: 12


QUE FAIT CETTE MIGRATION?
==========================

✅ Crée la table "batch_documents"
   - 12 colonnes
   - 8 types de documents
   - 6 lifecycle stages

✅ Crée 5 indexes pour performance
   - Sur batch_id
   - Sur document_type
   - Sur lifecycle_stage (partiel)
   - Sur created_at
   - Sur uploaded_by

✅ Active Row Level Security (RLS)
   - 4 policies pour la table
   - 4 policies pour le storage

✅ Crée storage bucket "batch-documents"
   - Privé et sécurisé

✅ Crée view "v_batch_documents_with_details"
   - Avec infos utilisateur et batch

✅ Crée trigger auto-update
   - Met à jour "updated_at" automatiquement


EN CAS D'ERREUR:
================

Erreur: "table already exists"
→ Normal si vous avez déjà tenté
→ La migration DROP la table d'abord
→ Ré-exécutez la migration complète

Erreur: "bucket already exists"  
→ Normal, ignorez et continuez
→ Le bucket existe déjà

Erreur: "policy already exists"
→ Normal si ré-exécution
→ La migration DROP les policies d'abord


APRÈS APPLICATION:
==================

1. ✅ Vérifier avec query ci-dessus
2. 📋 Service layer déjà créé (batchDocumentsService.ts)
3. 📋 Prochaine étape: Créer UI composant
4. 📋 Puis: Intégrer dans BatchDetailsWorkflow


TEMPS ESTIMÉ:
=============
⏱️  2 minutes pour appliquer migration
⏱️  2-3 heures pour UI + intégration


SUPPORT:
========
Si problème persiste, envoyez:
1. Le message d'erreur exact
2. Screenshot du SQL Editor


╔══════════════════════════════════════════════════════════════╗
║  STATUS: ✅ READY TO APPLY                                   ║
║  FICHIER: 20251105000001_create_batch_documents_fixed.sql    ║
╚══════════════════════════════════════════════════════════════╝
