╔══════════════════════════════════════════════════════════════════════════════╗
║         🔴 TRIGGER ERROR PERSISTS - ULTIMATE FIX                             ║
╚══════════════════════════════════════════════════════════════════════════════╝

ERROR:
  Error: Failed to run sql query: 
  ERROR: 42710: trigger "set_license_request_number" already exists

SITUATION:
  First cleanup didn't work. Trigger still exists.

ULTIMATE SOLUTION: 2 Steps Only


┌──────────────────────────────────────────────────────────────────────────────┐
│  STEP 1: FORCE DROP ALL LICENSE OBJECTS                                      │
└──────────────────────────────────────────────────────────────────────────────┘

  File: FORCE_DROP_ALL_LICENSE_OBJECTS.sql

  1. Open the file
  2. Copy ALL (CTRL+A, CTRL+C)
  3. Paste in Supabase SQL Editor
  4. Click "RUN"

  ⏱️ Wait 3-5 seconds

  This script is MORE AGGRESSIVE:
    • Loops through ALL triggers and drops them
    • Loops through ALL functions and drops them
    • Drops view with CASCADE
    • Drops ALL policies
    • Verifies everything is clean
    • Shows count of remaining objects

  At the end, you should see:
    ┌─────────────────────────────────────┐
    │ 🟢 PERFECT! Everything cleaned!     │
    │                                     │
    │ 🚀 NEXT STEP:                       │
    │    Now apply migration 19           │
    └─────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│  STEP 2: APPLY MIGRATION 19                                                  │
└──────────────────────────────────────────────────────────────────────────────┘

  File: 20251108000000_create_export_license_system.sql

  1. Open NEW SQL Editor tab
  2. Open migration file
  3. Copy ALL
  4. Paste in SQL Editor
  5. Click "RUN"

  ✅ Should complete WITHOUT errors this time!


┌──────────────────────────────────────────────────────────────────────────────┐
│  STEP 3: APPLY MIGRATION 20 (Sample Data)                                    │
└──────────────────────────────────────────────────────────────────────────────┘

  File: 20251108100000_seed_license_sample_data.sql

  1. Copy ALL
  2. Paste in SQL Editor
  3. Click "RUN"
  4. Should see: "INSERT 0 10"


┌──────────────────────────────────────────────────────────────────────────────┐
│  STEP 4: VERIFY & RELOAD                                                     │
└──────────────────────────────────────────────────────────────────────────────┘

  Run this test query:

  SELECT
    license_number,
    applicant_company_name,
    status
  FROM licenses_with_computed_fields
  LIMIT 3;

  ✅ Should return 3 rows

  Then:
  1. Go to /licenses page
  2. Press CTRL+SHIFT+R (hard reload)
  3. Should see 10 licenses + populated dropdown!


═══════════════════════════════════════════════════════════════════════════════
🔍 WHY FIRST CLEANUP DIDN'T WORK
═══════════════════════════════════════════════════════════════════════════════

  First script used simple DROP IF EXISTS statements.
  
  Problem: Trigger might be in weird state where it "exists" according to 
  Postgres but DROP IF EXISTS doesn't remove it.

  New script:
    ✓ Queries pg_trigger to find ALL triggers
    ✓ Loops through each and forces DROP with CASCADE
    ✓ Uses EXECUTE for dynamic SQL
    ✓ Catches and reports any errors
    ✓ Verifies at the end that count is 0


═══════════════════════════════════════════════════════════════════════════════
🐛 IF STILL GETTING TRIGGER ERROR
═══════════════════════════════════════════════════════════════════════════════

  After running FORCE_DROP script, check the output:

  Look for this line:
    "Triggers: 0 (should be 0)"

  If it says "Triggers: 1" or more:
    → A trigger is STILL there
    → Manually check which one:

  Run this query:

  SELECT tgname
  FROM pg_trigger t
  JOIN pg_class c ON t.tgrelid = c.oid
  WHERE c.relname = 'license_requests'
  AND tgname NOT LIKE 'RI_%';

  If it shows trigger name, drop it manually:

  DROP TRIGGER set_license_request_number ON license_requests CASCADE;


═══════════════════════════════════════════════════════════════════════════════
📁 FILES
═══════════════════════════════════════════════════════════════════════════════

  🔨 FORCE_DROP_ALL_LICENSE_OBJECTS.sql    ← RUN THIS FIRST!
  📄 20251108000000_create_export_license_system.sql (Migration 19)
  📄 20251108100000_seed_license_sample_data.sql (Migration 20)


═══════════════════════════════════════════════════════════════════════════════
✅ CHECKLIST
═══════════════════════════════════════════════════════════════════════════════

  □ Ran FORCE_DROP_ALL_LICENSE_OBJECTS.sql
  □ Verified output says "Triggers: 0"
  □ Applied migration 19 successfully (no errors!)
  □ Applied migration 20 (INSERT 0 10)
  □ Verified with SELECT query (3 rows)
  □ Reloaded /licenses page
  □ See 10 licenses in table
  □ Mining companies dropdown populated


═══════════════════════════════════════════════════════════════════════════════

🎯 This WILL work because the force drop script doesn't rely on simple 
   DROP IF EXISTS. It actively searches and destroys all license objects!

═══════════════════════════════════════════════════════════════════════════════
