================================================================================
                    DO YOU NEED TO RUN THE MIGRATION?
================================================================================

ANSWER: Only if you haven't run it yet OR if it failed!

================================================================================
QUICK CHECK: Did Migration Already Run Successfully?
================================================================================

Run this in Supabase SQL Editor:

-- Check if tables exist
SELECT tablename 
FROM pg_tables 
WHERE schemaname = 'public' 
  AND tablename IN ('assay_certificates', 'assay_base_metals', 'assay_deleterious_elements');

EXPECTED RESULT:
----------------
If you see 3 rows:
  assay_certificates
  assay_base_metals
  assay_deleterious_elements

= ✅ MIGRATION ALREADY RAN SUCCESSFULLY
= ❌ DO NOT RUN AGAIN

If you see 0 rows:
= ❌ MIGRATION NOT RUN YET
= ✅ RUN IT NOW

================================================================================
YOUR SITUATION
================================================================================

You said: "the previous migration which run successfully"

This means:
✅ Tables ALREADY created
✅ Migration ALREADY completed
❌ DO NOT run migration again (it will error)

================================================================================
WHAT TO DO NOW
================================================================================

SKIP THE MIGRATION! 

Instead, just create the storage bucket:

STEP 1: Go to Supabase Dashboard
STEP 2: Storage → New Bucket
STEP 3: Configure:
        Name: assay-certificates
        Public: OFF
        Size: 10485760
        MIME: application/pdf
STEP 4: Add policy (see below)

================================================================================
STORAGE BUCKET CREATION (Dashboard Method)
================================================================================

1. Supabase Dashboard → Storage (left sidebar)

2. Click "New bucket" button

3. Fill form:
   ┌─────────────────────────────────────────┐
   │ Name: assay-certificates                │
   │ Public bucket: □ (OFF - unchecked)      │
   │ File size limit: 10485760               │
   │ Allowed MIME types: application/pdf     │
   └─────────────────────────────────────────┘

4. Click "Create bucket"

5. Click on the new bucket → Policies tab

6. Click "New Policy" → "For full customization"

7. Paste this:
   ┌─────────────────────────────────────────────────────────────┐
   │ CREATE POLICY "Allow authenticated users all operations"    │
   │ ON storage.objects                                          │
   │ FOR ALL                                                     │
   │ TO authenticated                                            │
   │ USING (bucket_id = 'assay-certificates')                    │
   │ WITH CHECK (bucket_id = 'assay-certificates');             │
   └─────────────────────────────────────────────────────────────┘

8. Click "Review" → "Save policy"

9. ✅ DONE!

================================================================================
VERIFY BUCKET EXISTS
================================================================================

After creating, verify:

1. Dashboard → Storage
2. Look for: assay-certificates bucket
3. Should see it in the list

Or run this SQL:
SELECT id, name, public 
FROM storage.buckets 
WHERE id = 'assay-certificates';

EXPECTED:
id                  | name               | public
--------------------+--------------------+--------
assay-certificates  | assay-certificates | false

If you see this = ✅ Bucket exists!

================================================================================
TEST THE FEATURE
================================================================================

1. Refresh Gold Shipper app (F5)
2. Login
3. Batches → Any batch
4. Scroll down LEFT column
5. Find "Assay Certificates" section
6. Should see: Upload button with drag & drop zone
7. Try uploading a PDF

If upload works = ✅ SUCCESS!

================================================================================
SUMMARY: YOUR ACTION PLAN
================================================================================

Migration already successful? 
→ ✅ SKIP migration
→ ✅ Create storage bucket only (Dashboard method)
→ ✅ Test upload

Migration NOT run yet?
→ ✅ Run APPLY_ASSAY_MIGRATION_NOW.sql first
→ ✅ Then create storage bucket
→ ✅ Test upload

================================================================================
QUICK CHECKLIST
================================================================================

[ ] Migration ran successfully (you confirmed this)
[ ] Storage bucket created (assay-certificates)
[ ] Policy added (authenticated users)
[ ] App refreshed (F5)
[ ] Upload button visible
[ ] PDF upload works

Once all checked = 🎉 COMPLETE!

================================================================================
