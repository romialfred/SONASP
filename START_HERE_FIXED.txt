================================================================================
                   LICENSE REQUESTS - COMPLETE FIX
================================================================================

ROOT CAUSE DISCOVERED:
----------------------
The 500 Internal Server Error was caused by an RLS POLICY CONFLICT:

  1. license_requests table has RLS policy that checks user_profiles.role
  2. user_profiles table has RLS policy: USING (id = auth.uid())
     (Users can only see their OWN profile)
  3. When license_requests tries to check the role, it queries user_profiles
  4. The user_profiles RLS policy BLOCKS this query
  5. Result: 500 Internal Server Error!

This is a classic RLS recursion/lookup problem in Supabase.

ADDITIONAL ISSUE:
-----------------
The 'title' column is missing from license_requests table


THE SOLUTION:
-------------
1. Create SECURITY DEFINER functions that bypass RLS for role checks
2. Update all license-related RLS policies to use these functions
3. Add the missing 'title' column
4. Add performance indexes


HOW TO APPLY THE FIX:
=====================

STEP 1: Open Supabase Dashboard
  1. Go to: https://supabase.com/dashboard
  2. Select your project
  3. Click "SQL Editor" in left sidebar


STEP 2: Run the Complete Fix
  1. Click "New Query"
  2. Open file: FIX_LICENSE_REQUESTS_COMPLETE.sql
  3. Copy ENTIRE contents (Ctrl+A, Ctrl+C)
  4. Paste into SQL Editor (Ctrl+V)
  5. Click "Run" (or Ctrl+Enter)


STEP 3: Refresh Your Browser
  1. Go back to your app
  2. Press: Ctrl+Shift+R (hard refresh)
  3. Navigate to: License Management > License Requests
  4. DATA LOADS SUCCESSFULLY!


THIS IS THE DEFINITIVE FIX - RUN IT NOW!
