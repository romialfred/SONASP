╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║           CREATE LICENSE-DOCUMENTS BUCKET VIA DASHBOARD                     ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝


⚠️  SQL MIGRATION FAILED: Permission denied (must be owner of table buckets)

✅  SOLUTION: Create bucket through Supabase Dashboard UI instead


═══════════════════════════════════════════════════════════════════════════════
 📋 STEP-BY-STEP INSTRUCTIONS
═══════════════════════════════════════════════════════════════════════════════

STEP 1: OPEN STORAGE SETTINGS
  1. Open your Supabase Dashboard
  2. Click "Storage" in left sidebar
  3. Click "New bucket" button (top right)


STEP 2: CREATE BUCKET WITH THESE EXACT SETTINGS

  Bucket name:          license-documents
                        ↑ MUST BE EXACTLY THIS (lowercase, with hyphen)

  Public bucket:        ☐ OFF (unchecked)
                        ↑ Keep it private

  File size limit:      10 MB

  Allowed MIME types:   application/pdf
                        image/jpeg
                        image/jpg
                        image/png
                        ↑ Add each one separately


STEP 3: CONFIGURE RLS POLICIES

  After creating bucket, click on "license-documents" bucket

  Then go to "Policies" tab

  Click "New Policy" and create these 4 policies:


  ┌────────────────────────────────────────────────────────────────────┐
  │ POLICY 1: Allow Upload                                              │
  ├────────────────────────────────────────────────────────────────────┤
  │ Policy name:  Allow authenticated users to upload                   │
  │ Operation:    INSERT                                                 │
  │ Target roles: authenticated                                          │
  │ Policy:       WITH CHECK (bucket_id = 'license-documents')          │
  └────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────────────────┐
  │ POLICY 2: Allow View                                                │
  ├────────────────────────────────────────────────────────────────────┤
  │ Policy name:  Allow authenticated users to view                     │
  │ Operation:    SELECT                                                 │
  │ Target roles: authenticated                                          │
  │ Policy:       USING (bucket_id = 'license-documents')               │
  └────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────────────────┐
  │ POLICY 3: Allow Update Own Files                                    │
  ├────────────────────────────────────────────────────────────────────┤
  │ Policy name:  Allow users to update own files                       │
  │ Operation:    UPDATE                                                 │
  │ Target roles: authenticated                                          │
  │ USING:        bucket_id = 'license-documents' AND                   │
  │               auth.uid() = owner                                     │
  │ WITH CHECK:   bucket_id = 'license-documents' AND                   │
  │               auth.uid() = owner                                     │
  └────────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────────────────┐
  │ POLICY 4: Allow Delete Own Files                                    │
  ├────────────────────────────────────────────────────────────────────┤
  │ Policy name:  Allow users to delete own files                       │
  │ Operation:    DELETE                                                 │
  │ Target roles: authenticated                                          │
  │ Policy:       USING (bucket_id = 'license-documents' AND            │
  │                      auth.uid() = owner)                            │
  └────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
 ⚡ QUICK METHOD (If policies UI is confusing)
═══════════════════════════════════════════════════════════════════════════════

Just create the bucket with name: license-documents

Then run this SQL in SQL Editor to add policies:

-- Enable RLS on storage.objects (if not already enabled)
ALTER TABLE storage.objects ENABLE ROW LEVEL SECURITY;

-- Add policies for license-documents
CREATE POLICY "Allow authenticated upload to license-documents"
  ON storage.objects FOR INSERT
  TO authenticated
  WITH CHECK (bucket_id = 'license-documents');

CREATE POLICY "Allow authenticated view of license-documents"
  ON storage.objects FOR SELECT
  TO authenticated
  USING (bucket_id = 'license-documents');

CREATE POLICY "Allow users to update own license-documents"
  ON storage.objects FOR UPDATE
  TO authenticated
  USING (bucket_id = 'license-documents' AND auth.uid() = owner)
  WITH CHECK (bucket_id = 'license-documents' AND auth.uid() = owner);

CREATE POLICY "Allow users to delete own license-documents"
  ON storage.objects FOR DELETE
  TO authenticated
  USING (bucket_id = 'license-documents' AND auth.uid() = owner);


═══════════════════════════════════════════════════════════════════════════════
 ✅ VERIFICATION
═══════════════════════════════════════════════════════════════════════════════

After creating bucket, verify it exists:

1. Go to Storage → Buckets
2. You should see "license-documents" in the list

OR run this query in SQL Editor:

  SELECT name, public, file_size_limit, allowed_mime_types
  FROM storage.buckets
  WHERE name = 'license-documents';

Expected: 1 row returned


═══════════════════════════════════════════════════════════════════════════════
 🧪 TEST AFTER CREATION
═══════════════════════════════════════════════════════════════════════════════

1. Refresh your browser (Ctrl+Shift+R)
2. Go to: Licenses → Request New License
3. Fill step 1, click "Next"
4. Upload document in step 2
5. Click "Next"
6. ✅ Should work without "Bucket not found" error!


═══════════════════════════════════════════════════════════════════════════════
 📸 SCREENSHOTS OF WHERE TO CLICK
═══════════════════════════════════════════════════════════════════════════════

Dashboard → Storage (left sidebar) → New bucket (top right button)

Then fill:
  Name: license-documents
  Public: NO
  Size: 10 MB
  MIME: application/pdf, image/jpeg, image/jpg, image/png


═══════════════════════════════════════════════════════════════════════════════
 🎯 SUMMARY
═══════════════════════════════════════════════════════════════════════════════

❌ Can't create via SQL migration (permission denied)
✅ Create via Dashboard UI instead
✅ Name MUST be: license-documents (lowercase, with hyphen)
✅ Add RLS policies after creation
✅ Then refresh browser and test!


═══════════════════════════════════════════════════════════════════════════════
